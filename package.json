{
  "workspaces": [
    "lib",
    "demo"
  ],
  "scripts": {
    "dev": "npm run dev -w demo",
    "build": "npm run build --workspaces",
    "bundle": "npm pack -w lib",
    "publish": "npm publish -w lib"
  }
}
